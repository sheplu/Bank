export function logger(data) {
    // you need to log all errors and all requests
    // maybe some kind of mqtt to be able to handle everything
    // and why not and ELK or at least use logs to do monitoring
    //console.log(data);
};

export function loggerError(data) {
    // you need to log all errors and all requests
    // maybe some kind of mqtt to be able to handle everything
    // and why not and ELK or at least use logs to do monitoring
    //console.log(data);
}